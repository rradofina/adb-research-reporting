{
  "name": "adb-research-reporting",
  "version": "0.1.0",
  "type": "module",
  "description": "Next.js reporting site for the ADB Research governance stack (Constitution-governed, 17 programs).",
  "scripts": {
    "predev": "node -e \"const fs=require('node:fs'),cp=require('node:child_process'); for (const s of ['sync-articles','sync-evidence','sync-references','sync-docs','sync-topic-sprints','build-deepenings-index']) { if (fs.existsSync('../scripts/'+s+'.mjs')) cp.execSync('node ../scripts/'+s+'.mjs', {stdio:'inherit'}); else console.log('skip '+s+' (upstream scripts/ not present)'); }\"",
    "dev": "next dev --turbopack --port 5173",
    "prebuild": "node -e \"const fs=require('node:fs'),cp=require('node:child_process'); for (const s of ['sync-articles','sync-evidence','sync-references','sync-docs','sync-topic-sprints','build-deepenings-index']) { if (fs.existsSync('../scripts/'+s+'.mjs')) cp.execSync('node ../scripts/'+s+'.mjs', {stdio:'inherit'}); else console.log('skip '+s+' (upstream scripts/ not present)'); }\"",
    "build": "next build",
    "start": "next start --port 5174",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "next": "^16.2.9",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.2",
    "@types/node": "^26.0.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "autoprefixer": "^10.5.2",
    "postcss": "8.5.16",
    "tailwindcss": "^4.3.2",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "postcss": "8.5.16"
  }
}

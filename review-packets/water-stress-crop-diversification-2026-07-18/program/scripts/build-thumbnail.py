"""Compatibility wrapper for the construct-validation figure dossier.

The old thumbnail headlined an internal-resource ratio that did not support a
water-crop finding. The dossier now owns the honest hero and all article
figures. attestation_chain: ai-first.
"""

from __future__ import annotations

import runpy
from pathlib import Path


if __name__ == "__main__":
    runpy.run_path(
        str(Path(__file__).with_name("build-figure-dossier.py")),
        run_name="__main__",
    )
